<p><%= l(:mail_body_reminder_by_control_date, :count => @issues.size, :days => @days) %></p>
<ul>
<% @issues.each do |issue| %>
  <li><%= link_to_issue(issue, :project => true, :only_path => false) + ", Дата контроля: #{issue.custom_field_value(@field_id)}"%></li>
<% end -%>
</ul>